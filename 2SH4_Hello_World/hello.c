#include <stdio.h>
// This is the C’s standard IO library. 
// Required to put some text on screen.

int main(void) {
    printf("Hello World!"); // print something on the screen.
}

//type "cd 2SH4_Hello_World in terminal" to cd (change directory)

// type "gcc hello.c -o hello" in terminal
// gcc = gnu c compiler 
// it compiles the c program 'hello.c' and output it to an executable app called 'hello.exe'
// hello.exe is visible under the explorer's tab under the same subfolder


/* TO DO EACH TIME VSC OPENED
- open new terminal
- cd 2SH4_Heloo_World
- gcc hello.c -o hello
- ./hello
*/
